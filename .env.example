# Copy to .env and fill in (never commit .env)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional: signs the session cookie (defaults to the client secret)
SESSION_SECRET=
# Optional: canonical public origin for OAuth redirects
# PUBLIC_BASE_URL=http://127.0.0.1:5173
